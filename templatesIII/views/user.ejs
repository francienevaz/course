<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Usuário</title>
</head>
<body>
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        nav {
            padding: 50px;
            background-color: darkblue;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-family: Arial, Helvetica, sans-serif;
        }

        a {
            color: white;
            text-decoration: none;
            padding: 10px;
            border-radius: 10px;
            background-color: darkcyan;
        }

        .main {
            margin: 50px;
        }
    </style>
    <nav>
        <h1>Logo</h1>
        <div>
            <a href="/">Home</a>
            <a href="./about.ejs">About</a>
        </div>
        </div>
    </nav>
    <div class="main">
        <%# Abaixo fazemos um for para pegar os usuários de users e imprimir usando uma condição if para mmostrar
        somente os ids que forem par
        %>

        <% for (let user of users) {%>
            <% if(user.id % 2 == 0){%>
            <h1> <%=user.name%> </h1>
            <h4> <%=user.phone%> </h4>
            <%}%>
        <%}%>
    </div>
    
    
</body>
</html>