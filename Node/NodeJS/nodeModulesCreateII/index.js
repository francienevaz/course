let calc = require('matematica');

console.log(calc.area_triangulo(150, 46));


// Caso a pasta onde está as funções, ou seja, o pacote node que criamos seja apagada, basta usar o comando npm install, que o mesmo irá instalar o pacote novamente, sem a necessidade de colocar o endereço da pasta, pois no arquivo json já está instalado como dependências do projeto